
Comment Test file for use of => in forall;

forall c let a*c + u;

forall c let a*c = u;

forall c let a*c => u;

match a*c = u;

match a*c + u;

forall c match a*c = u;

forall c match a*c => u;

forall c match a*c + u;

end;
