#! /bin/sh
here="$0";while test -L "$here";do here=`ls -ld "$here" | sed 's/.*-> //'`;done
here=`cd \`dirname "$here"\` ; pwd`
here=`dirname "$here"`
exec $here/scripts/run.sh $here/scripts reduce reduce $*
