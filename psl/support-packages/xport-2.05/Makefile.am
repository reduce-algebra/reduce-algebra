# The "xport" program was used for fetching directories over the
# network in what now feels like the distant and hazy past. It is no
# longer used and no longer supported.


bin_PROGRAMS = xport

xport_SOURCES = xport.c

if exeext
p_xport = xport
$(p_xport):	xport$(EXEEXT)
	echo use \"make xport.exe\" please
endif

# end of Makefile
