(global (quote (nextsymbol show-new-ids kernel-maxsymbols old_symval 
old_symfnc old_symprp)))
(fluid (quote (code-base-hack symfnc symval old_symfnc old_symval 
kernel-maxsymbols tokenbuffer argumentblock)))
(fluid (quote (lastbps nextbps heaplast heaptrapbound heapupperbound 
heaptrapped nextsymbol using-other-bps-spaces*)))
(fluid (quote (kernel-maxsymbols)))
(fluid (quote (bpslowerbound nextbps)))
