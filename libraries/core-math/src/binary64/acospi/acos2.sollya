pretty = proc(u) {
  return ~(floor(u*1000)/1000);
};

d = [L,H];
f = acos(X0+x);
pf = (P0)+(P1)*x+(P2)*x^2+(P3)*x^3+(P4)*x^4+(P5)*x^5+(P6)*x^6;
err_p = -log2(dirtyinfnorm(pf-f, d));
print (pretty(err_p));
